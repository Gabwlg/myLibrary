# Issue Tracker: GitHub

Issues for this repository are tracked using GitHub Issues via the `gh` CLI.

## Workflow
- To create a new issue: `gh issue create --title "..." --body "..."`
- To list issues: `gh issue list`
- Skills like `to-tickets` and `to-spec` will use these commands.
